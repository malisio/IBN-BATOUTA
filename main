use std::io::{stdout, Write, BufWriter};

fn main() {
    let mut stdout = BufWriter::with_capacity(1000000, stdout().lock());
    let mut acc = 11719107999768421026u128;
    for i in 0.. {
        for _ in 0..94usize.pow(i as u32) {
            let _ = stdout.write_all(&(acc - 9404222468949967490).to_le_bytes()[..i]);
            let _ = stdout.write_all(&[10]);
            acc += 1 + ((!acc & 9259542123273814144) >> 7) * 162;
        }
    }
}
